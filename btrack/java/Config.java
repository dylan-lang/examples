package sigue.btrack;

import java.sql.*;

/**
 * Config is responsible for Bug Tracker configuration tasks
 * like safely setting up the the database tables when the
 * bug tracker is initially run.  The idea is that when the
 * administrator is installing Bug Tracker they should call
 * run this class as an application and it will prompt for
 * necessary information.
 */
public class Config {
    //---TODO
}
