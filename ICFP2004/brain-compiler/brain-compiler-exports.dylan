module: dylan-user

define library brain-compiler
  use Common-Dylan;
  use IO;
  use System;
  use ants;
end library;

define module assembler
  use Common-Dylan;
  use format-out;
  use ants;
end module;
