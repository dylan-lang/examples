module: dylan-user

define library validate
  use common-dylan;
  use io;
end library;

define module validate
  use common-dylan;
  use format;
  use format-out;
  use streams;
  use standard-io;
end module;
